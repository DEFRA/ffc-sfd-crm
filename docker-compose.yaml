services:
  ? ffc-sfd-crm
  : build:
      context: .
      target: production
    image: ffc-sfd-crm
    container_name: ffc-sfd-crm
    depends_on:
      - ffc-sfd-crm-postgres
    environment:
      APPINSIGHTS_CLOUDROLE: ffc-sfd-crm-local
      APPINSIGHTS_CONNECTIONSTRING: ${APPINSIGHTS_CONNECTIONSTRING}

